#include <stdio.h>
int main()
{
int a[5],sum,n,i;
printf("Enter the elements of the array");
scanf("%d",&n);
for(i=0;i<n;i++)
{
scanf("%d",&a[i]);
}
for(i=0;i<n;i++)
{
sum=sum+a[i];
}
printf("Sum of all the elements of an array: %d",sum);
return 0;
}
